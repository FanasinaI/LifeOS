// CSS imports (used for react-native-web styling, e.g. src/global.css) have no meaning under
// Jest — there's no bundler-level CSS loader here. Map them to an empty module instead of
// failing the parse.
module.exports = {};
